# Artefatos de instalação / runtime
backup/
backup*/
firmware/
work/
logs/
out/
*.bin
*.img
*.raw
*.zip
openstick-debian.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets / credenciais
.env
*.pem
id_rsa*
known_hosts

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Sessões do teste de campo (logs do dongle, dados pessoais)
testes/

# Backups de rádio por IMEI (ferramentas/backup_radio.py): binários com a
# calibração e a identidade de cada aparelho — nunca no git. Só o LEIAME vai.
!backups-radio/
backups-radio/*
!backups-radio/LEIAME.md
